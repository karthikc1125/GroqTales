# ============================================================================
# GROQTALES DOCKER ENVIRONMENT OVERRIDES
# ============================================================================
# This file contains Docker-specific environment variable overrides
# It works in conjunction with .env.local for Docker Compose
# Docker Compose loads both files, with .env.docker taking precedence
# ============================================================================

# Override local URLs for Docker container communication
NEXT_PUBLIC_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# MongoDB connection for Docker (uses service name from docker-compose)
# The actual URI is constructed in docker-compose.yml from this
MONGODB_DB_NAME=groqtales
DB_MAX_RETRIES=5
DB_RETRY_DELAY_MS=2000

# Redis configuration (update if you add Redis service)
REDIS_URL=redis://redis:6379

# Development settings for Docker
NODE_ENV=development
DEBUG=false
VERBOSE_LOGGING=false

# ============================================================================
# IMPORTANT: DO NOT COMMIT SECRETS
# ============================================================================
# Copy secrets from .env.local into this file only for local Docker development
# For production, use Docker secrets or your orchestration platform's secret management
# ============================================================================
